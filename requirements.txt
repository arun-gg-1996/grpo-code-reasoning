# existing
datasets==2.20.0
pyarrow==15.0.0
numpy<2.0.0
pyext>=0.7

# model + training
torch>=2.1.0
transformers>=4.40.0
trl>=0.8.6
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.43.0

# rollout generation
vllm>=0.4.0

# reference generation (Haiku + Sonnet scripts)
anthropic>=0.20.0

# utilities
tqdm
huggingface_hub>=0.22.0
httpx