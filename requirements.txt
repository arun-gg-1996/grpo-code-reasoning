# data
datasets>=4.0.0
pyarrow>=15.0.0
numpy<2.0.0
pyext>=0.7

# model + training
torch>=2.1.0
transformers>=4.40.0
trl>=0.15.0
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.43.0

# rollout generation
vllm>=0.4.0

# gemini judge (GCP Vertex AI)
google-auth>=2.27.0
google-cloud-aiplatform>=1.0.0
httpx>=0.28.0

# logging + tracking
wandb
huggingface_hub>=0.22.0

# utilities
python-dotenv
tqdm

# offline reference generation scripts only (not needed for training)
anthropic>=0.20.0
